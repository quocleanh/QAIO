
import { StyleSheet } from 'react-native';
import COLORS from '../consts/colors';

const genaral = StyleSheet.create({
  container: {
    flex: 1,
    justifyContent: 'center',
    alignItems: 'center',
    backgroundColor: 'red',
    padding: 16,
    top: 0,
    height: '100%',
    verticalAlign: 'middle',
  },
  
});

export default genaral;